// app/products/layout.jsx — server layout for /products
export const metadata = {
    title: "Products | HRP Industrial Products",
    description:
        "Browse 500+ industrial products across 11 categories including pneumatics, hydraulics, instrumentation, vacuum components and more.",
};

export default function ProductsLayout({ children }) {
    return children;
}
