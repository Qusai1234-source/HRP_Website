// app/contact/layout.jsx — server layout for /contact
export const metadata = {
    title: "Contact Us | HRP Industrial Products",
    description:
        "Get in touch with HRP for product inquiries, quotes, and technical support. Fast response guaranteed.",
};

export default function ContactLayout({ children }) {
    return children;
}
